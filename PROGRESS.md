# PROGRESS — PeachMint Browser Video Editor

## ▶ RESUME HERE
Phase 3 · step 3.27 · (next phase TBD)

## Pending revisions (do these FIRST on next `continue`, in order)
- [ ] (none)

## Task list   — status keys: [ ] todo  [~] in progress  [x] done  [!] paused/blocked

### Phase 0 — Foundations
- [x] 0.1 ARCHITECTURE.md — written
- [x] 0.2 Buildless ES-module scaffold + PWA shell + service worker + capability check screen + DEPENDENCIES.md
  - [x] 0.2.1 index.html (app entry, PWA meta, dark UI shell)
  - [x] 0.2.2 manifest.json (PWA manifest)
  - [x] 0.2.3 sw.js (service worker, offline/cache)
  - [x] 0.2.4 src/engine/capabilities.js (feature detection module)
  - [x] 0.2.5 src/ui/capability-panel.js (system check UI)
  - [x] 0.2.6 src/ui/app-shell.js (shell router desktop/mobile)
  - [x] 0.2.7 DEPENDENCIES.md
  - [x] 0.2.8 LICENSE, NOTICE, SECURITY.md, CONTRIBUTING.md, CODE_OF_CONDUCT.md
- [x] 0.3 Storage layer (OPFS + IndexedDB + persist) round-trip test
  - [x] 0.3.1 src/engine/storage.js (OPFS + IndexedDB abstraction)
  - [x] 0.3.2 src/engine/project.js (project data model, autosave stub)
  - [x] 0.3.3 src/engine/edl.js (EDL schema, keyframe model)
  - [x] 0.3.4 Storage round-trip smoke test in StorageLayer.selfTest() / capability panel

### Phase 1 — Playable core (MVP / P0)
- [x] 1.4 EDL model + multitrack timeline UI (desktop)
  - [x] 1.4.1 src/ui/desktop/shell.js — full desktop shell with start screen, project dialogs, keyboard shortcuts, transport
  - [x] 1.4.2 src/ui/desktop/timeline.js — multitrack timeline with ruler, playhead, clip drag, trim, track reorder (z-order)
  - [x] 1.4.3 src/ui/desktop/toolbar.js — tool selector, undo/redo, zoom, export stub
  - [x] 1.4.4 src/ui/desktop/inspector.js — clip/track properties, editable numeric props
  - [x] 1.4.5 src/ui/desktop/media-library.js — asset list, drag-to-timeline prep, import stub
  - [x] 1.4.6 app-shell.js updated to dynamically import real desktop shell
- [x] 1.5 WebCodecs decode → WebGL2 compositor → preview loop (one clip)
  - [x] 1.5.1 src/engine/compositor.js — WebGL2 pipeline (shaders, VAO, color correction, transform)
  - [x] 1.5.2 src/engine/decoder.js — ClipDecoder (HTMLVideoElement/Image, blob URL, seek-and-wait) + DecoderPool (LRU)
  - [x] 1.5.3 src/engine/preview-engine.js — RAF loop, scrub/play modes, preview:tick/ended events
  - [x] 1.5.4 shell.js — PreviewEngine wired to transport, project:dirty → timeline refresh
  - [x] 1.5.5 media-library.js — file import (picker + drag-drop), probe, OPFS write, Add to Timeline
- [x] 1.6 Import / trim / split / reorder / z-order / opacity+transform keyframes
  - [x] 1.6.1 edl.js — splitClip(), interpolate() for keyframe animation
  - [x] 1.6.2 preview-engine.js — resolveAnimatedProps() with keyframe interpolation per frame
  - [x] 1.6.3 inspector.js — keyframe add/delete UI (◆ button per property, keyframe list)
  - [x] 1.6.4 timeline.js — razor tool split, cross-track clip drag (ghost element), asset drop to lane, trimIn fix, trim-preview seek, keyframe diamond markers
  - [x] 1.6.5 shell.js — getCurrentTime wired to Inspector
- [x] 1.7 Web Audio: multi-clip, cueing, volume automation/fades
  - [x] 1.7.1 src/engine/audio-engine.js — AudioContext, AudioBufferSourceNode per clip, GainNode, buffer cache, async latency compensation, stop guard
  - [x] 1.7.2 Volume keyframe automation via AudioParam.linearRampToValueAtTime / setValueAtTime
  - [x] 1.7.3 shell.js — AudioEngine wired to play/stop/seek/project open/close
  - [x] 1.7.4 inspector.js — Volume propRow (0–2) with keyframe ◆ button
- [x] 1.8 Render targets + custom ratio + WebCodecs export (mp4-muxer)
  - [x] 1.8.1 src/engine/export-engine.js — ExportEngine (OffscreenCanvas + Compositor + DecoderPool, VideoEncoder H.264, AudioEncoder AAC, mp4-muxer ArrayBufferTarget)
  - [x] 1.8.2 toolbar.js — export button wired to onExport callback, enabled when project open
  - [x] 1.8.3 shell.js — _showExportDialog() with resolution/fps/bitrate selectors, progress bar + ETA, error display, MP4 download trigger
  - [x] 1.8.4 sw.js bumped to v6, export-engine.js added to APP_SHELL cache
- [x] 1.9 Autosave + project save/load + undo/redo
  - [x] 1.9.1 history.clear() on project open/close (prevents stale undo across projects)
  - [x] 1.9.2 Unsaved indicator: * suffix on project name via .pm-unsaved::after, wired to project:dirty/saved/autosaved
  - [x] 1.9.3 Inline project rename: double-click project name → editable input (Enter/Esc/blur commit)
  - [x] 1.9.4 Open project dialog: delete button per row with <dialog> confirmation, fix private storage access
  - [x] 1.9.5 sw.js bumped to v7
- [x] 1.10 Mobile UI shell over the same engine
  - [x] 1.10.1 src/ui/mobile/shell.js — MobileShell: header, canvas preview, transport, touch timeline (scroll/tap-seek/tap-select), bottom tab nav (Media/Clip/Export), menu sheet, project dialogs, import, undo/redo keyboard shortcuts
  - [x] 1.10.2 app-shell.js — dynamic import of mountMobileShell, removed placeholder
  - [x] 1.10.3 sw.js bumped to v8, mobile/shell.js added to APP_SHELL cache

### Phase 2 — Pro features (P1)
- [x] 2.11 Color correction + .cube/.3dl LUT import
- [x] 2.12 Chroma key + shape masks
- [x] 2.13 VFX shader library + open preset format + transitions
- [x] 2.14 Text + custom font import + cued static images
- [x] 2.15 Speed ramping + transparent WebM / GIF / PNG export
- [x] 2.16 Optional ffmpeg.wasm fallback (coi-serviceworker)

### Phase 3 — Advanced (P2)
- [x] 3.17 MediaPipe person/foreground segmentation → transparent bg
- [x] 3.18 Draw-over-frame / hand-drawn animation + onion-skinning
- [x] 3.19 Adjustment layers + experimental object tracking
- [x] 3.20 Multi-camera / multi-clip sync + waveform display
- [x] 3.21 Captions / subtitles — SRT/VTT import + on-canvas text-track overlay
- [x] 3.22 Speed curve editor — Bezier easing handles per keyframe in inspector
- [x] 3.23 Timeline snap — magnetic snap to clip edges, playhead, and grid while dragging/trimming
- [x] 3.24 Multi-track audio mixer — per-track volume/pan, master bus, VU meters
- [x] 3.25 Proxy media — generate lower-res preview proxies for large video files
- [x] 3.26 Timeline markers — named, colored markers on ruler with drag/rename/delete/keyboard navigation

## Decisions
- License: MIT
- ffmpeg.wasm: optional fallback (not default), WebCodecs primary
- Target devices: Desktop Chrome/Edge full suite; Android Chrome ~desktop; iOS Safari 16.4+ (degraded export)
- Fonts/LUTs: No bundled fonts (user-supplied); no bundled LUTs; open format for community packs

## Session log (append one entry per working session, newest at bottom)
- 2026-06-25 session 1 — did: Phase 0 complete — ARCHITECTURE.md, PROGRESS.md, index.html, manifest.json, sw.js, capabilities.js, storage.js, project.js, edl.js, history.js, app-shell.js, capability-panel.js, all OSS docs — committed + pushed · stopped at: end of Phase 0 · next: Phase 1.4 EDL model + multitrack timeline UI (desktop)
- 2026-06-25 session 2 — did: Phase 1.4 complete — shell.js (start screen, project dialogs, keyboard shortcuts, transport), timeline.js (ruler, playhead, clip drag/trim, track headers, z-order, zoom), toolbar.js, inspector.js, media-library.js, app-shell.js wired to real desktop shell · stopped at: end of 1.4 · next: Phase 1.5 WebCodecs decode → WebGL2 compositor → preview loop
- 2026-06-25 session 3 — did: Phase 1.5 complete — compositor.js (WebGL2 pipeline), decoder.js (ClipDecoder + DecoderPool), preview-engine.js (RAF loop, scrub/play modes), shell.js updated (PreviewEngine wired, project:dirty → timeline refresh, RAF-based play), media-library.js updated (full file import: picker + drag-drop, OPFS write, probe, Add to Timeline), app-shell.js async bug fixed · stopped at: end of 1.5 · next: Phase 1.6 Import / trim / split / reorder
- 2026-06-25 session 4 — did: Phase 1.6 complete — splitClip() in edl.js, resolveAnimatedProps() in preview-engine.js, inspector.js rewritten with keyframe ◆ buttons + keyframe list, timeline.js completed (razor split, cross-track ghost drag, asset-drop-to-lane, trimIn correction, trim-preview seek, keyframe diamond markers, _cmdSplitClip, _cmdAddClipFromAsset), sw.js bumped to v4 · stopped at: end of 1.6 · next: Phase 1.7 Web Audio
- 2026-06-25 session 5 — did: Phase 1.7 complete — audio-engine.js (AudioContext, AudioBufferSourceNode scheduling, GainNode volume, keyframe automation via AudioParam, buffer cache, async load + latency compensation, stop guard), inspector.js Volume propRow, shell.js AudioEngine wired (play/stop/seek/project events), sw.js bumped to v5 · stopped at: end of 1.7 · next: Phase 1.8 WebCodecs export
- 2026-06-25 session 6 — did: Phase 1.8 complete — export-engine.js (ExportEngine: OffscreenCanvas frame render, VideoEncoder H.264 AVCC, AudioEncoder AAC via OfflineAudioContext, mp4-muxer ArrayBufferTarget, abort/backpressure, keyframe interpolation), toolbar.js export button wired, shell.js _showExportDialog() (resolution/fps/bitrate UI, progress+ETA, error display, MP4 download), sw.js bumped to v6 · stopped at: end of 1.8 · next: Phase 1.9 autosave + save/load + undo/redo
- 2026-06-25 session 7 — did: Phase 1.9 complete — shell.js: history.clear() on project open/close, * unsaved indicator (project:dirty/saved/autosaved wired), inline project rename (dblclick name→input), open dialog delete per row (<dialog> confirm, _confirmDelete helper), fixed _storage private access, CSS .pm-unsaved::after + .pm-name-edit + .pm-proj-info + .pm-proj-del + .btn-danger, sw.js bumped to v7 · stopped at: end of 1.9 · next: Phase 1.10 mobile UI shell
- 2026-06-25 session 8 — did: Phase 1.10 complete — src/ui/mobile/shell.js (MobileShell: header+menu-sheet, canvas preview, touch transport, touch-scroll timeline with ruler/clips/playhead/tap-seek/clip-select, bottom tabs: Media library+import / Clip properties+delete / Export MP4 with progress, project dialogs, undo/redo keyboard shortcuts, injectStyles with full mobile CSS), app-shell.js updated to dynamically import mountMobileShell + removed placeholder, sw.js bumped to v8 · stopped at: end of Phase 1 · next: Phase 2.11 color correction + LUT
- 2026-06-25 session 9 — did: Phase 2.11 complete — src/engine/lut.js (parseCube, parse3dl, detectLUTFormat), compositor.js (FRAG shader upgraded with sampler3D u_lut + int u_lut_enabled, uploadLUT/setActiveLUT/disposeLUT, dummy 1×1×1 LUT on TEXTURE1, drawSolid explicit lutEnabled=0), preview-engine.js (LUT tex cache per assetId, _resolveLUT, setActiveLUT per clip, _clearLUTs on setProject/dispose), export-engine.js (same LUT loading with per-export lutCache map, resolveLUT helper), inspector.js (storage param, lutRow HTML, _onImportLUT/_ingestLUT/_onClearLUT/_showError, LUT CSS), shell.js (storage: this._storage → Inspector), sw.js bumped to v9 with lut.js · stopped at: end of Phase 2.11 · next: Phase 2.12 chroma key + shape masks
- 2026-06-25 session 10 — did: Phase 2.12 complete — compositor.js FRAG shader: chroma key (chroma-space distance, smoothstep alpha, u_chroma_enabled/color/threshold/smooth) + shape mask (SDF rect/ellipse, u_mask_type/center/size/feather/invert); drawClip sets all 9 new uniforms, drawSolid disables chroma+mask; inspector.js: Chroma Key section (enabled toggle, color picker, threshold/smoothness propRows) + Mask section (type select, x/y/w/h/feather propRows when active, invert toggle); _onChromaEnabledChange, _onChromaColorChange, _onMaskTypeChange, _onMaskInvertChange; hexToRgb/rgbToHex utilities; toggle/select/color-picker CSS · stopped at: end of Phase 2.12 · next: Phase 2.13 VFX shader library + transitions
- 2026-06-25 session 11 — did: Phase 2.13 complete — compositor.js: FRAG shader upgraded with 6 VFX uniforms (vignette, grain, sharpen, aberration, pixelate, time); pipeline order: pixelate→sample→aberration→sharpen→chroma→colorcorrect→LUT→vignette→grain→mask; drawClip(opacityMult=1) for transition blending, drawSolid zeros all VFX; setTime(t) method; new uniform locations in cache; edl.js: vfx defaults added to defaultClipProperties(), transitionClipsAtTime() + getTransitionOutFactor() exported; preview-engine.js: imports transitionClipsAtTime/getTransitionOutFactor, setTime() per frame, outgoing clips use getTransitionOutFactor as opacityMult, incoming clips rendered via transitionClipsAtTime with pre-roll media time; export-engine.js: same transition + setTime support; inspector.js: VFX section (5 propRows: vignette/grain/sharpen/aberration/pixelate), Transition section (type select + duration input, _onTransitionTypeChange/_onTransitionDurChange), Preset section (Save→.pmpreset download, Load→file picker apply, _onPresetSave/_onPresetLoad); setPropValue fixed to create missing path intermediates; preset CSS; sw.js bumped to v10 · stopped at: end of Phase 2.13 · next: Phase 2.14 Text + custom font import + cued static images
- 2026-06-25 session 12 — did: Phase 2.14 complete — text-renderer.js (NEW): TextRenderer renders text to HTMLCanvasElement via Canvas 2D (multi-line, bold/italic, custom font); edl.js: createAsset accepts fontFamily; preview-engine.js: TextRenderer + FontFace loading (_ensureFont, _getTextRenderer, _fontCache), text clips rendered in both active and transIn loops; export-engine.js: same TextRenderer + _ensureFont pattern for offline export, textRenderer passed to _renderFrame; inspector.js: Text section UI (textarea for content, font family input, fontSize/lineHeight propRows, color picker, align select, bold/italic toggles, _onTextPropChange), fixed 5 snapshotCommand bugs (_ingestLUT, _onClearLUT, _onMaskTypeChange, _onTransitionTypeChange, _onPresetLoad all now call history.execute(cmd)), textarea+row-col CSS; media-library.js: font file support (.ttf/.otf/.woff/.woff2 in picker+filter+probeFile, fontFamily in addAsset), T+ button → _addTextClip (creates overlay clip with text properties), assetIcon for font/lut; sw.js bumped to v11 with text-renderer.js · stopped at: end of Phase 2.14 · next: Phase 2.15 speed ramping + transparent WebM/GIF/PNG export
- 2026-06-26 session 14 — did: Phase 2.16 complete — sw.js: addCOIHeaders() patches COOP: same-origin + COEP: credentialless onto all same-origin responses enabling crossOriginIsolated=true for SharedArrayBuffer; bumped to v13 + added ffmpeg-engine.js to APP_SHELL; ffmpeg-engine.js (NEW): FFmpegEngine class loads @ffmpeg/ffmpeg + @ffmpeg/core@0.12.9 lazily from jsDelivr CDN, renders frames via Compositor+DecoderPool+TextRenderer (same pattern as ExportEngine), converts each frame via OffscreenCanvas.transferToImageBitmap() → 2D canvas → PNG blob → ffmpeg VFS, encodes MP4 (libx264 -preset fast) or WebM (libvpx), reads output ArrayBuffer, cleans VFS, static isAvailable checks SharedArrayBuffer+crossOriginIsolated; app-shell.js: COI reload — after SW registration, if !crossOriginIsolated, sets sessionStorage key + reloads once (either immediately if SW.controller != null, or on controllerchange event) so COI headers take effect; shell.js: export dialog gains #ex-ffmpeg-row panel (auto-shown when SAB available + format is mp4/webm) showing warning when WebCodecs unavailable (required path) or optional checkbox when WebCodecs works; startBtn handler branches to FFmpegEngine or ExportEngine based on webCodecsOK + checkbox; PROGRESS.md updated · stopped at: end of Phase 2.16 · next: Phase 3.17 MediaPipe segmentation
- 2026-06-26 session 20 — did: Phase 3.22 complete — edl.js: replaced simplified cubicBezierY with _solveCubicBezier() using 8-iteration Newton-Raphson (finds parametric t where x(t)=u, then returns y(t), properly handling CSS-style cubic-bezier); added ease-in (0.42,0,1,1), ease-out (0,0,0.58,1), ease-in-out (0.42,0,0.58,1) preset easing types to easeT(); inspector.js: buildKfList() expanded — each keyframe (except the first in each path) gains an easing <select> with 7 options (Linear/Smooth/Ease In/Ease Out/In-Out/Hold/Custom…), first keyframe shows '—' (its easing is unused — the outgoing segment convention stores easing on destination kf); when Custom easing is selected, a bezier row shows 4 inputs (x1,y1,x2,y2 with appropriate min/max for CSS control points including overshoot); _wireKfListControls() helper wires delete+easing+bezier events in both showClip and _refreshKfList; _onKfEasingChange() and _onKfHandleChange() undo-able setters with _refreshKfList call on easing type change; CSS for pm-kf-easing-sel/pm-kf-bezier-row/pm-kf-easing-na; sw.js bumped to v19 · stopped at: end of Phase 3.22 · next: Phase 3.23 Timeline snap
- 2026-06-26 session 19 — did: Phase 3.21 complete — captions.js (NEW): parseSRT(), parseVTT() (block-based parsers, SRT sequence# skip, VTT header/NOTE/STYLE/REGION skip, HTML tag strip), findActiveCue(cues,t) linear scan, renderCaptionToCanvas(text,opts,fw,fh) OffscreenCanvas+HTMLCanvasElement fallback (font size scaled to 1920 reference, word-wrap via _wrapText, centered bottom layout, rounded-rect semi-transparent background via _roundRect); media-library.js: .srt/.vtt added to file picker accept + filter, caption import path (file.text() parse → addAsset with captions:[] + duration = last cue end, no OPFS write), _addToTimeline caption branch (overlay track, clip.properties.caption defaults), assetIcon '💬' + assetMeta cue count; preview-engine.js + export-engine.js: import findActiveCue+renderCaptionToCanvas, caption branch in both active+transIn loops (asset.type==='caption' check after asset lookup, before storageKey guard, computes clipTime→findActiveCue→renderCaptionToCanvas→drawClip); inspector.js: isCaptionClip detection, Caption Style section (cue count, fontSize number input, color picker, fontFamily text input), _setCaptionProp() undo-able setter, caption control wiring in showClip, pm-cap-color CSS; sw.js bumped to v18 + captions.js in APP_SHELL · stopped at: end of Phase 3.21 · next: Phase 3.22 Speed curve editor
- 2026-06-26 session 18 — did: Phase 3.20 complete — waveform.js (NEW): WaveformCache class using OfflineAudioContext.decodeAudioData → RMS peaks (Float32Array, normalized, 400 samples), peakFileTime() for sync alignment, getCached() sync fast-path, pending deduplication; timeline.js: setWaveformCache() API, _buildClipEl appends <canvas class="pm-tl-waveform"> per audio/video clip (absolute inset, drawn below label via DOM prepend, async paint on cache resolve, maps trimIn+speed to peak slice), Ctrl/Meta+click multi-select (addToSelection param to _selectClip, _selectedClipIds Set, multi-selected CSS class, _updateSyncBtn shows/hides "Sync" button at ≥2), syncSelectedClips() public method (peak→local→absolute time alignment, anchor=first, history command with full undo, returns bool), corner Sync button (CSS, disabled until ≥2 selected); shell.js: WaveformCache import, _waveformCache field, WaveformCache constructed after storage init + wired to timeline.setWaveformCache(), Ctrl+Shift+S shortcut calls syncSelectedClips(); sw.js bumped to v17 + waveform.js in APP_SHELL · stopped at: end of Phase 3.20 · next: Phase 3.21 Captions / subtitles
- 2026-06-26 session 17 — did: Phase 3.19 complete — compositor.js: applyAdjustment(props,w,h) method using gl.copyTexImage2D to capture framebuffer into _adjTex (lazy-created TEXTURE2D), then re-renders through full color/VFX pipeline with gl.ONE/gl.ZERO replace blend (no self-compositing); _adjTex in constructor + dispose(); tracker.js (NEW): TrackingEngine.track() reads asset from OPFS, creates hidden <video>, samples at ≤10fps at 25% scale, SSD template matching (32×32 patch, ±64px search window, grayscale, early-exit SSD loop), updates template each frame for appearance adaptation, returns {time,x,y}[] in canvas-pixel-from-centre coords; preview-engine.js: adjustment clip branch (props.adjustment===true) in active loop → resolveLUT + applyAdjustment; export-engine.js: same branch in _renderFrame active loop; media-library.js: Adj button → _addAdjClip() (overlay track, 10s, adjustment:true); inspector.js: onTrackRequest callback option, isAdjClip detection, Adjustment Layer info section, Track Point button on video clips; shell.js: _enterTrackingPickMode(clip) banner+crosshair pick, _cancelTrackingMode, _runTracking(clip,nx,ny) progress dialog + TrackingEngine lazy import, _showTrackApplyDialog(kfs) clip picker, _applyTrackingKfs(clip,kfs) history command; CSS .pm-track-banner; sw.js bumped to v16 + tracker.js in APP_SHELL · stopped at: end of Phase 3.19 · next: Phase 3.20 Multi-camera / multi-clip sync + waveform display
- 2026-06-26 session 16 — did: Phase 3.18 complete — draw-renderer.js (NEW): DrawRenderer class with renderFrame() (shared canvas, quadratic bezier smoothing, single-point arc dot, normalized 0-1 coords) + paintStrokes() (onto existing ctx with alpha multiplier) + _paintStroke helper (destination-out eraser support); edl.js: getDrawFrameIdx(clip,t) export (floor(localTime×fps)); preview-engine.js: imports DrawRenderer+getDrawFrameIdx, _getDrawRenderer() lazy factory, _drawRenderer in constructor+dispose, draw clip branch in both active+transIn render loops (renderFrame→drawClip with project dims); export-engine.js: same pattern, DrawRenderer created per-export path (video/PNG/GIF), dispose in finally, _renderFrame gets drawRenderer=null param, both clip loops handle draw clips; toolbar.js: ✏ draw tool button (D shortcut); media-library.js: ✏+ button → _addDrawClip() (overlay track, 10s clip, drawing:{fps:12,frames:{}}); inspector.js: isDrawClip detection, Draw Layer section (FPS input, frame count, Clear all button), _onDrawFpsChange/_onDrawClearAll handlers, AI Segmentation hidden for draw clips; shell.js: imports DrawRenderer+getDrawFrameIdx, draw state in constructor, onToolChange wires _setDrawMode, _onClipSelect tracks _drawClip, _onProjectOpened syncs overlay canvas dimensions, _setDrawMode(active) shows/hides overlay+controls, _bindDrawOverlay() (pointer capture, normalized coords, live stroke), _bindDrawControls() (pen/eraser/color/width/opacity/onion toggles), _redrawOverlay(live) (onion skins blue/red + committed strokes + live stroke), _commitStroke/_clearDrawFrame (history commands), _stepDrawFrame(±1), _currentDrawFrameIdx(), _updateDrawFrameInfo(); HTML: overlay canvas + draw controls panel; CSS: pm-draw-overlay absolute inset, draw-controls flex bar with nav/tool/color/width/opacity/onion/clear; sw.js bumped to v15 + draw-renderer.js in APP_SHELL · stopped at: end of Phase 3.18 · next: Phase 3.19 Adjustment layers + experimental object tracking
- 2026-06-26 session 15 — did: Phase 3.17 complete — segmentation.js (NEW): SegmentationEngine lazy-loads @mediapipe/tasks-vision@0.10.22 from jsDelivr CDN (IMAGE mode, GPU delegate with silent fallback), selfie_segmentation.tflite model from Google storage, confidenceMasks[1]=person confidence, returns {mask:Float32Array,width,height}; compositor.js: FRAG shader gains u_seg_mask (sampler2D TEXTURE2) + u_seg_enabled + u_seg_feather + u_seg_invert, seg pass after chroma key uses smoothstep(0.5±sf) on mask.r × s.a; constructor adds _segTex/_dummySeg/_segEnabled; init() creates 1×1 R8 dummy seg on TEXTURE2, caches 4 seg uniform locs; drawClip() binds _segTex or _dummySeg + sets 4 seg uniforms from props.seg; drawSolid() sets segEnabled=0; setSegmentationMask(maskFloat,w,h)/clearSegmentationMask() new API; dispose() cleans segTex+dummySeg; edl.js: seg:{enabled:false,feather:0.02,invert:false} added to defaultClipProperties(); preview-engine.js: _segEngine lazy import, _ensureSegEngine(), dispose() cleans segEngine, _renderFrame calls segment() before drawClip when props.seg.enabled, clearSegmentationMask() otherwise (both active+transition loops); export-engine.js: _renderFrame gains segEngine=null param, needsSeg check before export creates SegmentationEngine, segEngine.dispose() in finally, wired in all 3 export paths (video/PNG/GIF); inspector.js: AI Segmentation section (enabled toggle, feather propRow, invert toggle, hint text), _onSegEnabledChange()/_onSegInvertChange() handlers; sw.js bumped to v14 + segmentation.js in APP_SHELL · stopped at: end of Phase 3.17 · next: Phase 3.18 Draw-over-frame / hand-drawn animation + onion-skinning
- 2026-06-26 session 24 — did: Phase 3.26 complete — Timeline markers: edl.js: markers:[] added to createProject(), createMarker({time,label,color})/addMarker()/removeMarker() exports; timeline.js: import createMarker+removeMarker, addMarker(time) public method (creates marker, sorts, markDirty, drawRuler, renderMarkerLines, shows rename input), contextmenu listener on ruler canvas (right-click near flag → _cmdDeleteMarker), dblclick listener (near flag → _showMarkerRenameInput), marker flag drawing in _drawRuler() (triangle 0-9px height + dashed vertical line + truncated label), marker lane line divs in _renderLanes() (pm-tl-marker-line class, colored borderLeft, translateX), _markerAtRulerX() (8px threshold, top 14px y zone), _renderMarkerLines() (remove+recreate all lines, insert before playhead), _updateMarkerLines() (update translateX on live drag), marker drag in _onRulerPointerDown (detect vs seek, store origMarkerTime), _onPointerMove (real-time position update), _onPointerUp (undo-able move command), _cmdDeleteMarker (undo-able delete), _showMarkerRenameInput (floating <input> on ruler-scroll, blur/Enter/Escape handlers, undo-able rename); shell.js: Ctrl+Left/Right → jump to prev/next marker (before ArrowLeft/Right handlers, with return), M key → timeline.addMarker(currentTime); sw.js bumped to v23 · stopped at: end of Phase 3.26 · next: Phase 3.27
- 2026-06-26 session 23 — did: Phase 3.25 complete — proxy-engine.js (NEW): ProxyEngine.isAvailable checks SAB+crossOriginIsolated, generate() reads source via storage.readMedia, writes to ffmpeg VFS, transcodes to 854×480 H.264 -preset ultrafast -crf 28 + AAC 96k, parses Duration/time= log lines for 0→1 progress, stores output via storage.writeMedia, returns proxyKey; ff.on/off('log') for cleanup; preview-engine.js: _proxyAsset(asset) helper returns synthetic wrapper {…asset, storageKey:asset.proxyKey, id:asset.id+'__proxy'} when proxyKey exists, else returns asset unchanged; both getDecoder(asset) calls (active loop line 262 + transIn loop line 330) now use getDecoder(this._proxyAsset(asset)); media-library.js: PROXY_ENGINE_URL lazy import URL, _proxyInProgress Map<assetId,number> in constructor; _proxyButtonHtml() renders one of 3 states: P button (enabled/disabled based on SAB check), X% busy, ✓P done+removable; _handleProxyClick() either removes proxy (delete storage + clear asset.proxyKey) or starts _generateProxy(); _generateProxy() lazy imports ProxyEngine via window._proxyEngineModule, calls eng.generate with progress callback updating _proxyInProgress + _refreshProxyBtn(), on completion sets asset.proxyKey + markDirty; _refreshProxyBtn() replaces just the .pm-lib-asset-proxy button in DOM without re-rendering full list; CSS: .pm-lib-asset-proxy base (18px, hidden until hover, mono font), --busy (always visible, purple), --done (mint border, red on hover); sw.js bumped to v22 + proxy-engine.js in APP_SHELL · stopped at: end of Phase 3.25 · next: Phase 3.26
- 2026-06-26 session 22 — did: Phase 3.24 complete — edl.js: volume:1.0+pan:0.0 added to createTrack(); audio-engine.js: full audio graph restructure — _buildMasterBus() creates master GainNode + AnalyserNode (fftSize=256) chained to destination; _ensureTrackNodes() creates per-track GainNode+StereoPannerNode+AnalyserNode chain (overlay tracks skipped); _disconnectTrackNodes() tears down cleanly; _scheduleClip() routes through track node → master; new public API: setMasterVolume/setTrackVolume/setTrackPan/getMasterAnalyser/getTrackAnalyser; setProject clears track nodes; timeline.js: AUDIO_TRACK_H=56, VIDEO_TRACK_H=72, OVERLAY_TRACK_H=56; _audioEngine/_vuRafId/_vuBuf in constructor; setAudioEngine(engine) public method; setProject calls _startVuLoop/_stopVuLoop; _renderHeaders: hasMixer flag for audio/video tracks, 2-row column layout (pm-tl-track-header--mix + pm-tl-th-top + pm-tl-th-mixer), VU canvas (44×6) + vol range (0–1.5) + pan range (-1…1) + vol% label, sliders wired to track.volume/pan + audioEngine live; trackHeight updated for overlay; _startVuLoop/_stopVuLoop/_paintVuMeters() per-track VU RAF; snap-line bug fixed in _renderLanes (preserved alongside playhead on re-render); CSS for mixer strip/sliders/VU; shell.js: _masterVuRafId/_masterVuBuf in constructor; setAudioEngine wired to timeline; master vol slider + VU canvas (60×10) added to transport bar; _bindTransport wires #pm-master-vol → audioEngine.setMasterVolume(); _startMasterVuLoop/_stopMasterVuLoop methods started in _onProjectOpened/_onProjectClosed; CSS for .pm-master-vol-slider/.pm-master-vu; sw.js bumped to v21 · stopped at: end of Phase 3.24 · next: Phase 3.25 Proxy media
- 2026-06-26 session 21 — did: Phase 3.23 complete — timeline.js: _snapEnabled flag (constructor), Snap toggle button (#pm-tl-snap-toggle, .active class, click handler) in corner controls, snap line element (#pm-tl-snap-line absolute overlay in lanes-inner); _onPointerMove clip drag: both left+right edges tested via _findSnap, closer wins, newStart adjusted, _showSnapLine called; _onPointerMove trim-right: proposedEnd snapped; _onPointerMove trim-left: newStart snapped; _onPointerUp: _hideSnapLine() after drag clear; _collectSnapTimes(excludeClipId): collects playhead + all clip edges (excl. dragged clip), computes _snapFrameInterval (frame grid active when frameW≥3px); _findSnap(proposedSec,snapTimes): pixel-distance comparison against SNAP_THRESHOLD_PX, also tests nearest frame-grid position; _showSnapLine(time)/hideSnapLine(): translateX snap-line into view; Alt key temporarily disables snap during any drag; CSS for .pm-tl-snap-toggle+.active + .pm-tl-snap-line; sw.js bumped to v20 · stopped at: end of Phase 3.23 · next: Phase 3.24 Multi-track audio mixer
- 2026-06-26 session 13 — did: Phase 2.15 complete — compositor.js: init({transparent}) alpha canvas option, blendFuncSeparate for transparent mode, clear(r,g,b,a) alpha param, getPixels() readback with vertical flip; export-engine.js: export() dispatcher by format (mp4/webm/gif/png), _exportVideo() with webm-muxer + VP9 branch (WEBM_MUXER_CDN), _exportPNG() (OffscreenCanvas transparent mode + convertToBlob), _exportGIF() (gifenc library: quantize+applyPalette per frame, GIFENC_CDN), _renderFrame transparent param; inspector.js: Speed read-only → editable input (pm-speed-input, _onSpeedChange with undo, clamped 0.1–10×); shell.js: _showExportDialog() rewritten with format selector (MP4/WebM/GIF/PNG), per-format option panels (video bitrate/fps, GIF fps, PNG time+transparent checkbox), download branches on format MIME type + extension; sw.js bumped to v12 · stopped at: end of Phase 2.15 · next: Phase 2.16 ffmpeg.wasm fallback
